# Research Narrative 网站退役记录

状态：**永久退役，禁止重新使用或部署。**

该目录保存曾部署为 `human-infra.pages.dev` 的 Astro Research Narrative 网站源码、同源根目录静态页、配置和历史发布工作流，仅供追溯。Cloudflare Pages 项目 `human-infra` 已删除。

## 禁止事项

- 禁止执行该目录中的 `dev`、`build`、`preview`、`pages deploy` 或等价命令。
- 禁止恢复 `.github/workflows/pages.yml`。
- 禁止重新创建名为 `human-infra` 的 Cloudflare Pages 项目来发布此归档。
- 禁止把这里的页面、布局、样式、静态资源或发布配置复制回活动目录。
- 禁止恢复根目录 `singularity-human-infra.html` 静态入口。
- 禁止把本归档作为科技树、Wiki 或其他公开产品的基础。

## 保留原因

保留源码只为保存问题来源、历史决策和回滚证据。归档不是候选方案，不进入 CI，不参与任何生产构建。
